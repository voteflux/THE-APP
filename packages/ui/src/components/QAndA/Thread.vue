<template>
    
</template>

<script>

    // this.reply_ids.do({
    //     success: (rids) => {
    //         Promise.all(R.map(async (rid) => {
    //             this.replies[rid] = WebRequest.Loading()
    //             this.replies[rid] = await this.$flux.v3.qanda.getReply(rid)
    //         }, rids as string[]))
    //     }
    // })
export default Vue.extend({
})
</script>

<style scoped>

</style>